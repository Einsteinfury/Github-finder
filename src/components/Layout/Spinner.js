import React, {Fragment} from 'react';
import './spinner.css';

const Spinner = () => {
  return (
    <Fragment>
      <div className="spinner circle"></div>  
    </Fragment>
  );
};

export default Spinner
